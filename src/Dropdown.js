import React, {useState} from 'react';
import {Container} from 'reactstrap';

const Dropdown = () => {
    
    
  //  let randomGame = Array.filter(hm =>  `${hm.genre}` === `${genre}` );
  //  console.log(randomGame);

  //   let x = (Math.floor((Math.random()) * randomGame.length ));
  //   console.log((randomGame[x]).game);
  //   randomGame.splice(x, 1);  
      

  //   let y = (Math.floor((Math.random()) * (randomGame.length -1) + 1));
  //   // console.log((randomGame[y]).game);
  //   randomGame.splice(y, 1);
    
  //   let z = (Math.floor((Math.random()) * (randomGame.length -1) + 1));
  //   // console.log((randomGame[z]).game);
  //   randomGame.splice(z, 1); 

  //   let a = (Math.floor((Math.random()) * (randomGame.length -1) + 1));
  //   // console.log((randomGame[a]).game);
  //   randomGame.splice(a, 1);

  //   let b = (Math.floor((Math.random()) * (randomGame.length -1) + 1));
  //   // console.log((randomGame[b]).game);
  //   randomGame.splice(b, 1);

    return (
       
    );

    }
  
  

export default Dropdown;
